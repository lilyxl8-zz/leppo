<nav class="topnav group">
  <ul class="topnav-menu-left">
    <figure class="topnav-brand">
      <a class="topnav-brand-logo" href="/#"><i>l</i></a>
    </figure>
    <li>
      <a href="#"><i class="fa fa-folder-o"></i> Following</a>
    </li>
    <li>
      <a href="#/countries"><i class="fa fa-flag-o"></i> Countries</a>
    </li>
    <% if signed_in? %>
    <li>
      <a href="#/liked"><i class="fa fa-heart-o"></i> Liked</a>
    </li>
    <% end %>
  </ul>
  <ul class="topnav-menu-right">
  <% if signed_in? %>
    <li>
      <button class='new-feed navbar-btn btn new-category hvr-radial-out'>
        +
      </button>
    </li>
    <li>
      <%= button_to "Sign out", session_url, method: :delete, class: "navbar-btn btn hvr-radial-out" %>
    </li>

  <% else %>
    <li>
      <a href="/session/new">
        <button class='navbar-btn btn hvr-radial-out login-btn'>
          Log in
        </button>
      </a>
    </li>
    <li>
      <a href="/users/new" style="padding: 0;">
        <button class='navbar-btn btn hvr-radial-out'>
          Sign up
        </button>
      </a>
    </li>
  <% end %>
  </ul>
</nav>

<%= flash[:errors] %>
<% if flash[:errors] %>
  <div class="errors">
    <%= flash[:errors].join("<br>").html_safe %>
  </div>
<% end %>
